{
 "name": "compsci-101-visualised",
 "version": "1.0.0",
 "description": "Visualisations for popular algorithms and data structures",
 "main": "src/index.js",
 "repository": "git@github.com:mmckeaveney/compsci-101-visualised.git",
 "author": "mmckeaveney <martinmckeaveney@gmail.com>",
 "scripts": {
  "start": "light-server -s . -p 7000",
  "build": "rollup -c",
  "dev": "rollup -c -w",
  "test": "jest --ci --colors --verbose -c=jest.config.json -b",
  "test:cov": "jest --ci --colors --verbose -c=jest.config.json -b --coverage",
  "test:watch": "jest --colors --ci --verbose --watch -b -c=jest.config.json",
  "test:update": "jest --colors -c=jest.config.json --coverage -u",
  "format": "prettier --tab-width 2 --write '{src,__tests__}/**/*.js'"
 },
 "license": "MIT",
 "devDependencies": {
  "babel-jest": "^20.0.3",
  "babel-preset-es2015": "^6.24.1",
  "babel-preset-stage-0": "^6.24.1",
  "jest": "^20.0.4",
  "prettier": "^1.5.3",
  "rollup": "^0.43.0",
  "rollup-plugin-babel": "^2.7.1",
  "rollup-plugin-fill-html": "^1.0.2",
  "rollup-plugin-livereload": "^0.4.0",
  "rollup-plugin-serve": "^0.3.0",
  "rollup-watch": "^4.0.0"
 }
}
